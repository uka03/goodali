class DownloadedItem {
  String? banner;
  String? name;
  String? albumName;
  String? productId;
  String? body;

  DownloadedItem(
      {this.banner, this.albumName, this.name, this.productId, this.body});
}
